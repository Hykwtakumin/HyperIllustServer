import * as React from "react";
import * as ReactDOM from "react-dom";
import { useState, useRef, FC, useEffect } from "react";

export const topPage: React.FC<{}> = ({}) => {
  return (
    <div>
      <h1>Hello SSR!</h1>
      <p>under construction</p>
    </div>
  );
};

//render(<topPage />, document.body);
